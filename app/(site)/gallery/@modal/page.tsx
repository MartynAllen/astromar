// Matches the @modal slot when the URL is exactly /gallery (e.g. after
// closing the lightbox via router.back()) so the modal doesn't stay mounted.
export default function Page() {
  return null;
}
